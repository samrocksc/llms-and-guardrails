// Generated by dts-bundle-generator v9.5.1

import { TypeDeclarationFileSpecifier, TypeDeclarationLibSpecifier, TypeDeclarationPackageSpecifier } from 'ts-declaration-location';
import ts from 'typescript';

/**
 * The immutability values are sorted in ascending order.
 */
export declare enum Immutability {
	Unknown,
	Mutable = 2,
	ReadonlyShallow = 3,
	ReadonlyDeep = 4,
	Immutable = 5,
	Calculating
}
type PatternSpecifier = {
	name?: string | string[];
	ignoreName?: string | string[];
	pattern?: RegExp | RegExp[];
	ignorePattern?: RegExp | RegExp[];
} & ({
	name: string | string[];
} | {
	pattern: RegExp | RegExp[];
});
type FileSpecifier = PatternSpecifier & TypeDeclarationFileSpecifier;
type LibSpecifier = PatternSpecifier & TypeDeclarationLibSpecifier;
type PackageSpecifier = PatternSpecifier & TypeDeclarationPackageSpecifier;
/**
 * How a type can be specified.
 */
export type TypeSpecifier = string | RegExp | FileSpecifier | LibSpecifier | PackageSpecifier;
/**
 * Check if a type matches a specifier.
 */
export type TypeMatchesPatternSpecifier = (program: ts.Program, type: ts.Type, typeNode: ts.TypeNode | null, include: ReadonlyArray<string | RegExp>, exclude: ReadonlyArray<string | RegExp>) => boolean;
/**
 * A list of immutability overrides.
 */
export type ImmutabilityOverrides = ReadonlyArray<{
	type: TypeSpecifier;
	to: Immutability;
	from?: Immutability;
}>;
/**
 * Get the default overrides that are applied.
 */
export declare function getDefaultOverrides(): ImmutabilityOverrides;
/**
 * A cache used to keep track of what types have already been calculated.
 */
export type ImmutabilityCache = WeakMap<object, Immutability>;
/**
 * Get the immutability of the given type.
 *
 * If you only care about the immutability up to a certain point, a
 * `maxImmutability` can be specified to help improve performance.
 *
 * @param program - The TypeScript Program to use.
 * @param typeOrTypeNode - The type to test the immutability of.
 * @param overrides - The overrides to use when calculating the immutability.
 * @param useCache - Either a custom cache to use, `true` to use the global
 * cache, or `false` to not use any predefined cache.
 * @param maxImmutability - If set then any return value equal to or greater
 * than this value will state the type's minimum immutability rather than it's
 * actual. This allows for early-escapes to be made in the type calculation.
 * @param typeMatchesPatternSpecifier - Allows for overriding how we check if a
 * type matches a pattern. This is used for checking if an override should be
 * applied or not.
 */
export declare function getTypeImmutability(program: ts.Program, typeOrTypeNode: ts.Type | ts.TypeNode, overrides?: ImmutabilityOverrides, useCache?: ImmutabilityCache | boolean, maxImmutability?: Immutability, typeMatchesPatternSpecifier?: TypeMatchesPatternSpecifier): Immutability;
/**
 * Get the minimum immutability from the given values.
 *
 * Note: Unknown immutability will be ignore; thus Unknown will be return if
 * and only if all values are Unknown.
 */
export declare function min(a: Immutability, b: Immutability): Immutability;
/**
 * Get the maximum immutability from the given values.
 *
 * Note: Unknown immutability will be ignore; thus Unknown will be return if
 * and only if all values are Unknown.
 */
export declare function max(a: Immutability, b: Immutability): Immutability;
/**
 * Clamp the immutability between min and max.
 */
export declare function clamp(minValue: Immutability, value: Immutability, maxValue: Immutability): number;
/**
 * Is the given immutability immutable?
 */
export declare function isImmutable(immutability: Immutability): boolean;
/**
 * Is the given immutability at least ReadonlyDeep?
 */
export declare function isReadonlyDeep(immutability: Immutability): boolean;
/**
 * Is the given immutability at least ReadonlyShallow?
 */
export declare function isReadonlyShallow(immutability: Immutability): boolean;
/**
 * Is the given immutability Mutable?
 */
export declare function isMutable(immutability: Immutability): boolean;
/**
 * Is the given immutability unknown?
 */
export declare function isUnknown(value: Immutability): boolean;
/**
 * Is the immutability of the given type immutable.
 *
 * @param program - The TypeScript Program to use.
 * @param typeOrTypeNode - The type to test the immutability of.
 * @param overrides - The overrides to use when calculating the immutability.
 * @param useCache - Either a custom cache to use, `true` to use the global
 * cache, or `false` to not use any predefined cache.
 */
export declare function isImmutableType(program: ts.Program, typeOrTypeNode: ts.Type | ts.TypeNode, overrides?: ImmutabilityOverrides, useCache?: ImmutabilityCache | boolean): boolean;
/**
 * Is the immutability of the given type at least readonly deep.
 *
 * @param program - The TypeScript Program to use.
 * @param typeOrTypeNode - The type to test the immutability of.
 * @param overrides - The overrides to use when calculating the immutability.
 * @param useCache - Either a custom cache to use, `true` to use the global
 * cache, or `false` to not use any predefined cache.
 */
export declare function isReadonlyDeepType(program: ts.Program, typeOrTypeNode: ts.Type | ts.TypeNode, overrides?: ImmutabilityOverrides, useCache?: ImmutabilityCache | boolean): boolean;
/**
 * Is the immutability of the given type at least readonly shallow.
 *
 * @param program - The TypeScript Program to use.
 * @param typeOrTypeNode - The type to test the immutability of.
 * @param overrides - The overrides to use when calculating the immutability.
 * @param useCache - Either a custom cache to use, `true` to use the global
 * cache, or `false` to not use any predefined cache.
 */
export declare function isReadonlyShallowType(program: ts.Program, typeOrTypeNode: ts.Type | ts.TypeNode, overrides?: ImmutabilityOverrides, useCache?: ImmutabilityCache | boolean): boolean;
/**
 * Is the immutability of the given type mutable.
 *
 * @param program - The TypeScript Program to use.
 * @param typeOrTypeNode - The type to test the immutability of.
 * @param overrides - The overrides to use when calculating the immutability.
 * @param useCache - Either a custom cache to use, `true` to use the global
 * cache, or `false` to not use any predefined cache.
 */
export declare function isMutableType(program: ts.Program, typeOrTypeNode: ts.Type | ts.TypeNode, overrides?: ImmutabilityOverrides, useCache?: ImmutabilityCache | boolean): boolean;

export {};
